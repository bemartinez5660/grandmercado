import { PaymentHostDirective } from './payment-host.directive';

describe('PaymentHostDirective', () => {
  it('should create an instance', () => {
    const directive = new PaymentHostDirective();
    expect(directive).toBeTruthy();
  });
});
